# CloudStation CLI Environment Variables
# =====================================
# These can be set at runtime to override baked-in defaults

# Main API URL (cs-backend)
CS_API_URL=https://cst-cs-backend-gmlyovvq.cloud-station.io

# Auth Service URL (cs-auth)
CS_AUTH_URL=https://cs-auth.cloud-station.io

# Configuration file path
CS_CONFIG=cloudstation.hcl

# Log level (trace, debug, info, warn, error)
CS_LOG_LEVEL=info

# Non-interactive mode (skip prompts)
CS_NON_INTERACTIVE=false

# Use remote builds
CS_REMOTE=false

# =====================================
# GitHub Actions Secrets (for CI/CD)
# =====================================
# Set these in GitHub repo Settings > Secrets > Actions
#
# CS_API_URL    - Override default API URL in release builds
# CS_AUTH_URL   - Override default Auth URL in release builds
#
# To release:
#   1. Push a tag: git tag v1.0.0 && git push origin v1.0.0
#   2. Or manually trigger workflow with version input
